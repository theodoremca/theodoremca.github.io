import { createStore } from 'vuex'
import services from './services'

export default createStore({
  namespace:true,
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    services 
  }
})
